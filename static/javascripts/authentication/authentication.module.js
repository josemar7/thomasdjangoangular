(function () {
  'use strict';

  angular
    .module('thomas.authentication', [
      'thomas.authentication.controllers',
      'thomas.authentication.services'
    ]);

  angular
    .module('thomas.authentication.controllers', []);

  angular
    .module('thomas.authentication.services', []);
})();